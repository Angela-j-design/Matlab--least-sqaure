# Matlab--least-sqaure
This project implements curve fitting and prediction using MATLAB and the least squares method. Linear, quadratic, and square-root models are constructed and compared using sum of squared errors (SSE). The best-fitting model is used to analyze and predict tensile strength over time.
